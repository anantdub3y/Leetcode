class Solution {
public:
    int minStartValue(vector<int>& nums) {
        int sum = 0, minSum = 0;
        for (int n : nums) {
            sum += n;
            if (sum < minSum) minSum = sum;
        }
        return 1 - minSum;
    }
};
